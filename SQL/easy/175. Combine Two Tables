```sql
-- LeetCode 175. Combine Two Tables
-- https://leetcode.com/problems/combine-two-tables

-- Problem: Report each person’s first name, last name, city, and state. 
-- If no address exists, show NULL.
-- Approach: Use LEFT JOIN on personId.

SELECT p.lastName, p.firstName, a.city, a.state
FROM Person p
LEFT JOIN Address a 
  ON p.personId = a.personId;
```
